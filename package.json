{
  "name": "darwins-garage",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Watch a population of randomly-shaped vehicles evolve into fast, working cars via a real genetic algorithm and live 2D physics.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test test/",
    "lint": "eslint src test"
  },
  "dependencies": {
    "matter-js": "^0.20.0"
  },
  "devDependencies": {
    "eslint": "^9.9.0",
    "vite": "^8.1.5"
  },
  "engines": {
    "node": ">=20"
  }
}
